apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rw-metastore-secret
  namespace: cnpg-system
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: rw-metastore-secret
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: "c2819f29-de3f-4b10-bd81-b3e7015fee88"
