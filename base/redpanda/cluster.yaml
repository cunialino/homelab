apiVersion: cluster.redpanda.com/v1alpha2
kind: Redpanda
metadata:
  name: redpanda
  annotations:
    argocd.argoproj.io/sync-wave: "5" 
spec:
  clusterSpec:
    statefulset:
      initContainers:
        setDataDirOwnership:
          enabled: true
    tls:
      enabled: false
